workflows:
  pr_workflow:
    name: Run on Pull Request
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: main
          include: true
        - pattern: '*'
          include: true
    scripts:
      - name: Check Code Formatting
        script: |
          dart format --set-exit-if-changed lib/